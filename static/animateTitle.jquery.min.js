(function($){var animateTitle_interval=null,animateTitle_title=document.title,animateTitle_idx=0;var methods={clear:function(){clearInterval(animateTitle_interval);document.title=animateTitle_title;animateTitle_idx=0;animateTitle_interval=null;},create:function(text,time){document.title=text[0];animateTitle_interval=setInterval(function(){methods.swap(text);},time);},swap:function(text){animateTitle_idx++;document.title=text[animateTitle_idx%text.length];}};$.fn.animateTitle=function(options,time){if(options=="clear"){methods.clear();return this;}var settings={'timeout':1000,'text':[],};if(typeof options!="object"){$.error('This plugin does not support these parameters in the constructor');return this;}if(typeof options.text!='undefined'){$.extend(settings,options);}else{settings.text=options;}if(settings.text.length==0){$.error('Array has no members.');return this;}if(typeof time=='number'){settings.timeout=time;}methods.clear();methods.create(settings.text,settings.timeout);return this;};$.extend({animateTitle:$.fn.animateTitle});})(jQuery);
